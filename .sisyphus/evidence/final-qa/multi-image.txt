QA Evidence: Multi-Image Path Parsing
=======================================
Date: 2026-04-17
Scenario: Test 6 from MANUAL-TEST-SUBAGENT-SECURITY.md

OBJECTIVE
---------
Verify that comma-separated path parsing for vision subagent works correctly.
"img1.png,img2.png" should create Vec of 2 paths.

CODE UNDER TEST
---------------
File: src/tools/subagent_tools.rs, test_parse_comma_separated_paths (lines 339-345)

Parsing logic:
  let input = "img1.png,img2.jpg";
  let paths: Vec<PathBuf> = input.split(',').map(|s| PathBuf::from(s.trim())).collect();

This parsing is used in the spawn_subagent tool for vision type, where the
file_path parameter can contain comma-separated paths for multi-image support.

UNIT TEST RUN
-------------
$ cargo test --all-features subagent_tools::tests::test_parse_comma_separated_paths -- --nocapture

Result: PASS ✓

Test code:
  let input = "img1.png,img2.jpg";
  let paths: Vec<PathBuf> = input.split(',').map(|s| PathBuf::from(s.trim())).collect();
  assert_eq!(paths.len(), 2);
  assert_eq!(paths[0], PathBuf::from("img1.png"));
  assert_eq!(paths[1], PathBuf::from("img2.jpg"));

VERIFICATION:
- Input "img1.png,img2.jpg" → splits on ',' → ["img1.png", "img2.jpg"]
- .trim() on each ensures no whitespace issues
- Vec length = 2 ✓
- Path contents match exactly ✓

ADDITIONAL VERIFICATION:
Each parsed path then goes through validate_subagent_path() for:
1. CWD sandbox validation
2. Blocklist check
3. Existence check

The validate_subagent_paths() function (src/security.rs, lines 121-130) validates
all paths in the Vec, returning Err on first failure.

SECURITY NOTE:
Multi-image paths are validated INDIVIDUALLY. If any path in the comma-separated
list is outside CWD or matches blocklist, the entire operation fails with an error.

VERDICT: PASS
=============
Comma-separated path parsing correctly creates Vec of 2 paths.
All paths validated through security sandbox.