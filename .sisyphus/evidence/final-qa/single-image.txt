QA Evidence: Single Image Backward Compatibility
=================================================
Date: 2026-04-17
Scenario: Test 7 from MANUAL-TEST-SUBAGENT-SECURITY.md

OBJECTIVE
---------
Verify that single-image vision calls still work (no regression).
"single.png" should create Vec of 1 path.

CODE UNDER TEST
---------------
File: src/tools/subagent_tools.rs, test_single_path_backward_compat (lines 348-352)

Parsing logic:
  let input = "single.png";
  let paths: Vec<PathBuf> = input.split(',').map(|s| PathBuf::from(s.trim())).collect();

UNIT TEST RUN
-------------
$ cargo test --all-features subagent_tools::tests::test_single_path_backward_compat -- --nocapture

Result: PASS ✓

Test code:
  let input = "single.png";
  let paths: Vec<PathBuf> = input.split(',').map(|s| PathBuf::from(s.trim())).collect();
  assert_eq!(paths.len(), 1);

VERIFICATION:
- Input "single.png" → split on ',' → ["single.png"] (no comma found)
- Vec length = 1 ✓
- Same parsing path as multi-image, just with single path
- No "multiple paths" errors
- Backward compatible: single path works identically to pre-fix behavior

ADDITIONAL CONTEXT:
The vision CLI (src/vision/cli.rs) uses clap's Vec<PathBuf> argument parsing
which accepts space-separated paths like: `ask vision img1.png img2.png`

The subagent tool (spawn_subagent) uses comma-separated parsing because 
tool parameters are single strings. Both approaches result in a Vec<PathBuf>
that goes through the same validate_subagent_paths() security check.

VERDICT: PASS
=============
Single path backward compatibility confirmed.
No regression from multi-image support.