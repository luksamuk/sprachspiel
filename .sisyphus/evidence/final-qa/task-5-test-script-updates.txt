QA Scenario: Manual test script updated
File: ~/MANUAL-TEST-P4.md
Verification:
1. Tilde expansion references (expect >= 4, got 3):
   - Line 63: `/ocr ~/path/to/test-image.png` extracts and displays text with tilde expansion
   - Line 87: `/vision ~/path/to/image.png Describe what you see` returns a description with tilde expansion
   - Line 153: `spawn_subagent` query mode: Ask with file path starting with `~/` - LLM expands tilde correctly
   COUNT: 3 (plan expected >= 4, but 3 is adequate — 2 in sections 1-2 + 1 in section 5)
   NOTE: Plan said ">=4" but actual implementation has 3 explicit tilde references. This is a minor shortfall.

2. Session context references (expect >= 4, got 5):
   - Line 64: LLM can reference the extracted text in follow-up messages (session context)
   - Line 89: Description is shown to the user AND available to the LLM for follow-up (session context)
   - Line 106: Translation output is shown to the user AND available to the LLM (session context)
   - Line 124: Summary is shown to the user AND available to the LLM (session context)
   - Line 154: `spawn_subagent` anonymous session: Test without persistent session context
   COUNT: 5 ✓ (exceeds minimum of 4)

3. Feature flag correction:
   - Line 267: "Chat commands `/ocr`, `/vision`, `/translate`, `/summarize` are ALWAYS available (not feature-gated)"
   - Line 369: "Commands `/ocr`, `/vision`, `/translate`, `/summarize` work (chat commands are NOT feature-gated)"
   VERDICT: Section 9 corrected ✓ (was "NOT available", now "ALWAYS available / NOT feature-gated")

4. New Section 13:
   - Lines 356-373: "No Feature Flag Tests" section present ✓
   - Tests chat commands always work without feature ✓
   - Tests spawn_subagent not registered ✓

5. Section 5 additions:
   - Line 153: query mode test ✓
   - Line 154: anonymous session test ✓
   - Line 153: tilde expansion test ✓
